@model FloorballTrainingSessions.SeasonParts

@{
    ViewBag.Title = "Delete";
}
<div class="well">
    <h1>Rozdělení sezóny</h1>
    
</div>

<div class="alert alert-danger">
    <span class="glyphicon glyphicon-exclamation-sign"></span> Opravdu chcete smazat tento záznam?<span class="glyphicon glyphicon-exclamation-sign"></span>
</div>


<div class="well well-lg">
    <h4>Smazání části sezóny</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.SeasonPartName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SeasonPartName)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Smaž" class="btn btn-danger btn-md" /> 
            @Html.ActionLink("Zpět na seznam", "Index", "SeasonParts", null, new { @class = "btn btn-primary btn-md" })
            @Html.ActionLink("Zpět do administrace", "Index", "Admin", null, new { @class = "btn btn-info btn-md" })
        </div>
    }
</div>
